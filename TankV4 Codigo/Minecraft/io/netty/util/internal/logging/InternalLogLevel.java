package io.netty.util.internal.logging;

public enum InternalLogLevel {
   TRACE,
   DEBUG,
   INFO,
   WARN,
   ERROR;

   private static final InternalLogLevel[] $VALUES = new InternalLogLevel[]{TRACE, DEBUG, INFO, WARN, ERROR};
}
